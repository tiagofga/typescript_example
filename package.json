{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "compile": "tsc",
    "start": "npm run compile && node build/index.js ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "typescript": "^4.6.4"
  }
}
